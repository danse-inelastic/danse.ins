#!/usr/bin/env python

from distutils.core import setup
setup(name='${PKG_NAME}',
      version='${VERSION}',
      description='${SUMMARY}',
      author='${AUTHOR}',
      author_email='${AUTHOR_EMAIL}',
      url="${GITHUB_URL}",
      packages=['danse', 'danse.ins'],
     )
